node_modules/
.env
output/
cache/
# SQLite store for the GUI's template library (server/db.ts) - the live/query
# copy. templates/*.json (its git-tracked export) is intentionally NOT
# ignored - that's what gets pushed to GitHub, see server/templates.ts.
data/
gui/dist/
assets/music/*
!assets/music/.gitkeep
assets/tts/*
!assets/tts/.gitkeep
# User-uploaded image layer assets (LayerEditor.tsx's image layer upload, server/index.ts's /api/assets/images) - not meant for git.
assets/images/*
!assets/images/.gitkeep
src/compositions/sample-data.json
# Original upload staging copy - real copies now live under assets/
audio-samples/
